{
  "version": 2,
  "builds": [
    { "src": "vercel_handler.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "vercel_handler.py" }
  ],
  "env": {
    "DJANGO_SETTINGS_MODULE": "core.settings"
  }
}
